package string;

import java.util.Scanner;

public class removeduplicates {
    public static void main(String[] args) {
        Scanner obj=new Scanner(System.in);
        System.out.println("enter the string");
        String abcd=obj.nextLine();
        String result=removeduplicate(abcd);
        System.out.println("the removed duplicates is:"+result);
        obj.close();
    }
    public static string remove duplicates(string str)
}
